/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PaymentForBookingComponent } from './payment-for-booking.component';

describe('PaymentForBookingComponent', () => {
  let component: PaymentForBookingComponent;
  let fixture: ComponentFixture<PaymentForBookingComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [PaymentForBookingComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(PaymentForBookingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */